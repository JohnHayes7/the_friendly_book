import React from 'react'
import './logout.css'

export default class Logout extends React.Component {
    render(){
        return(
            <div id="logout-window">
                Logout Window
            </div>
        )
    }
}